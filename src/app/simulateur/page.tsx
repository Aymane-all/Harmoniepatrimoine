import Simulateur from "@/components/simulateur/Simulateur";

export const metadata = {
  title: "Simulateur 2026 — Vos aides en 2 minutes",
};

export default function SimulateurPage() {
  return <Simulateur />;
}
